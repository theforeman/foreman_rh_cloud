name: Assert Single Commit (non-blocking)

on: pull_request

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Commit Count Check
      run: test ${{ github.event.pull_request.commits }} = 1
